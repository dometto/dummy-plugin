- name: Dummy Plugin
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Try updating apt
      apt:
        update_cache: true

    - name: Try installing package
      package:
        name: python3-jmespath
        state: present

    - name: Get all packages as facts
      package_facts:
        manager: auto
